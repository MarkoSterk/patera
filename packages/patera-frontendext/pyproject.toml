[project]
name = "patera-frontendext"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = [
  "patera",
  "pyodide-py>=0.27.7",
  "pyscript>=0.3.3",
  "patera-frontend"
]

[tool.uv.sources]
patera = { workspace = true }

[tool.uv.build-backend]
module-name = "patera.frontendext"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
