[project]
name = "pyjolt-taskmanager"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "apscheduler>=3.11.2",
  "pyjolt",
]

[tool.uv.sources]
pyjolt = { workspace = true }

[tool.uv.build-backend]
module-name = "pyjolt.taskmanager"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
